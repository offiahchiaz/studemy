<% layout('layout') %>

<div class="container">
    <div class="row">
        <% courses.forEach((course) => { %> 
            <div class="col-md-4">
                <a href="/course/<%= course._id %>">
                    <div class="thumbnail">
                        <img src="<%= course.image %>" alt="">
                        <div class="caption">
                            <h3><%= course.name %></h3>
                            <p><%= course.category.name %></p>
                            <p>₦<%= course.price %></p>
                        </div>
                    </div>
                </a>
            </div>
        <% }) %>
    </div>
</div>